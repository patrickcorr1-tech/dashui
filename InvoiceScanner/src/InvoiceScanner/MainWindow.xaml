<Window x:Class="InvoiceScanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InvoiceScanner" Height="440" Width="760" WindowStartupLocation="CenterScreen" Background="#0B0E14">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="InvoiceScanner" FontSize="28" FontWeight="SemiBold" Foreground="#E6EDF3" />
        <TextBlock Grid.Row="1" Margin="0,8,0,20" Text="Manual sync from Outlook → OCR → Rename → Move to Documents\Scannedfiles" Foreground="#9FB1C5" />

        <Border Grid.Row="2" Background="#121826" CornerRadius="14" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="SyncButton" Content="Sync Now" Width="140" Height="38" Style="{StaticResource PrimaryButton}" Click="SyncNow_Click" />
                    <Button Content="Hide" Width="100" Height="38" Margin="12,0,0,0" Style="{StaticResource SecondaryButton}" Click="Hide_Click" />
                </StackPanel>

                <TextBlock Grid.Row="1" Margin="0,12,0,6" Text="Status" Foreground="#9FB1C5" />
                <TextBox Grid.Row="2" x:Name="LogBox" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="#0F1522" Foreground="#D7E0EA" BorderBrush="#1E2A3B" />
            </Grid>
        </Border>
    </Grid>
</Window>
